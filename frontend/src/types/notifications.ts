// Interface for pending toasts response
export interface PendingToastResponse {
    id: string
    title: string
    message: string
    type: string
    created_at: string
    reference_id: string
}